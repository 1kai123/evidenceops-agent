# EvidenceOps Agent 中文提交指南

这份指南用于把项目发布到 GitHub，并提交到线上黑客松。

## 当前项目状态

- 项目名：EvidenceOps Agent
- 本地目录：`C:\Users\ZMC\Desktop\TEST\evidenceops-agent`
- 本地 Demo：`http://127.0.0.1:5177`
- 压缩包：`C:\Users\ZMC\Desktop\TEST\evidenceops-agent.zip`
- 最新 Git commit：`6182120 Add SIFT DFIR integration plan`

## 第一步：创建 GitHub 仓库

打开：

```text
https://github.com/new
```

填写：

- Repository name：`evidenceops-agent`
- Description：`Explainable AI incident response workspace for hackathon submission.`
- Visibility：`Public`
- 不要勾选 `Add a README file`
- 不要勾选 `Add .gitignore`
- 不要勾选 `Choose a license`

创建后，把仓库 URL 发给 Codex。

## 第二步：推送代码

仓库创建后，运行以下命令。把 `YOUR_USERNAME` 换成你的 GitHub 用户名。

```powershell
cd C:\Users\ZMC\Desktop\TEST\evidenceops-agent
git remote add origin https://github.com/YOUR_USERNAME/evidenceops-agent.git
git branch -M main
git push -u origin main
```

如果已经加过 remote，则用：

```powershell
git remote set-url origin https://github.com/YOUR_USERNAME/evidenceops-agent.git
git push -u origin main
```

## 第三步：部署 Demo

优先用 GitHub Pages：

1. 打开仓库 Settings。
2. 打开 Pages。
3. Source 选择 `GitHub Actions`。
4. 等待 Actions 运行完成。

Demo 链接大概率是：

```text
https://YOUR_USERNAME.github.io/evidenceops-agent/
```

备选：Netlify。

1. 打开 Netlify。
2. 直接拖入 `evidenceops-agent` 文件夹或连接 GitHub 仓库。
3. 得到 Netlify Demo URL。

## 第四步：录制 Demo 视频

使用 `VIDEO_SHOTLIST.md`。

建议录制流程：

1. 打开 Demo 页面。
2. 选择 `OAuth Token Theft`。
3. 点击 `Run agent`。
4. 展示 Risk、Confidence、Evidence Timeline、Agent Reasoning。
5. 在 Human Approval Queue 点击一次 `Mark approved`。
6. 展示底部生成的 Submission Brief。
7. 简短说明后续可接 Splunk、MCP、SIFT/DFIR。

视频控制在 2 分 30 秒左右。

## 第五步：优先提交的平台

### 1. Band of Agents

最适合当前作品。把项目包装成企业多 Agent 安全响应工作流。

### 2. Splunk Agentic Ops Hackathon

也适合。强调 `SPLUNK_INTEGRATION.md` 里的 SPL 查询和 Splunk MCP 映射。

### 3. FIND EVIL!

奖金高，但要求更偏 DFIR/SIFT。当前已有 `SIFT_DFIR_INTEGRATION.md`、`DATASET.md`、`ACCURACY_REPORT.md`、`EXECUTION_LOG_SAMPLE.md`，但最好后续再补真实或半真实 DFIR 数据。

## 第六步：提交表单字段

优先复制 `DEVPOST_ANSWERS.md`。

常见字段：

- Project name：`EvidenceOps Agent`
- Tagline：`Explainable AI incident response with evidence timelines, multi-agent reasoning, and human approval gates.`
- Built with：`HTML, CSS, JavaScript, mock SIEM telemetry, agent workflow design`
- GitHub URL：填仓库链接
- Demo URL：填 GitHub Pages 或 Netlify 链接
- Video URL：填公开视频链接

## 注意

注册账号、创建仓库、公开发布代码、提交报名表、上传视频都属于对外动作。Codex 可以帮你准备和填写，但最终提交前需要你确认。
